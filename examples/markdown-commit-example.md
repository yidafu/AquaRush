# Markdown 格式 Commit 示例

## 更新内容

更新后的 `generate-commit.sh` 脚本现在支持生成 Markdown 格式的 commit 消息，包含以下功能：

### 🎯 主要特性

1. **智能分析**：自动分析暂存文件并建议 commit 类型和范围
2. **Markdown 格式**：生成的 commit 消息包含完整的 Markdown 格式
3. **详细统计**：自动生成按模块分类的变更统计
4. **影响评估**：包含文件数量、代码行数变更等统计信息
5. **测试清单**：为功能类型 commit 自动生成测试要求清单

### 📊 Commit 结构

生成的 commit 消息包含以下部分：

```markdown
feat(user,order): implement user authentication and order processing

## 📊 Changes Summary

- **User Module**: 5 files modified
- **Order Module**: 3 files modified
- **Common Module**: 2 files modified

## 📈 Impact

- **Files Changed**: 10
- **Lines Added**: 150
- **Lines Removed**: 25

## 🧪 Testing Requirements

- [ ] Unit tests pass
- [ ] Integration tests pass
- [ ] Manual testing completed
```

### 🔧 使用方法

```bash
# 暂存文件
git add modules/aqua-user/src/main/kotlin/dev/yidafu/aqua/user/service/AuthService.kt

# 运行 commit 脚本
./scripts/generate-commit.sh

# 或者使用斜杠命令
/commit
```

### 📋 交互式流程

1. **分析暂存文件**：脚本自动分析变更的文件
2. **建议类型和范围**：基于文件内容建议 commit 类型（feat/fix/docs/refactor/chore）
3. **输入标题**：用户输入遵循 Angular 格式的 commit 标题
4. **可选详细描述**：可选择输入详细的 commit 描述
5. **自动生成摘要**：如果没有提供描述，自动生成变更摘要
6. **创建 commit**：使用 Markdown 格式创建 commit

### 🎨 Markdown 功能

- **标题层级**：使用 `##` 创建二级标题
- **粗体文本**：使用 `**` 突出显示重要信息
- **代码块**：自动格式化代码相关内容
- **列表项**：使用 `-` 创建清晰的项目列表
- **表情符号**：使用相关表情符号增强可读性

### 📈 支持的模块分类

- **User Module**: 用户管理和认证相关
- **Order Module**: 订单处理和管理
- **Delivery Module**: 配送员和任务管理
- **Product Module**: 产品目录和库存
- **Payment Module**: 支付处理和交易
- **Common Module**: 共享工具和基础设施
- **Services**: 服务层配置和结构
- **Frontend**: 客户端应用和界面
- **Schema**: 数据库模式和迁移

这种改进的 commit 格式使项目历史更加清晰、详细，便于团队协作和代码审查。