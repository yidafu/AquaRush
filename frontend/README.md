# 前端项目结构说明

本项目包含 3 个前端应用：用户端小程序、配送员端小程序和管理后台。

## 📱 1. 用户端小程序 (user-client)

### 技术栈
- **框架**: Remax (React for 微信小程序)
- **语言**: TypeScript + React
- **构建**: Remax CLI

### 页面结构
```
src/
├── pages/
│   ├── home/              # 首页 - 产品列表
│   ├── product-detail/    # 产品详情
│   ├── order-confirm/     # 确认订单
│   ├── my/                # 我的
│   ├── address-list/      # 地址列表
│   ├── address-edit/      # 地址编辑
│   ├── order-list/        # 订单列表
│   └── order-detail/      # 订单详情
├── components/            # 公共组件
├── services/              # API 服务
│   ├── product.ts        # 产品 API
│   └── order.ts          # 订单 API
├── utils/                 # 工具函数
│   └── request.ts        # 请求封装
├── app.tsx               # 应用入口
└── app.config.ts         # 小程序配置
```

### 启动命令
```bash
cd frontend/user-client
npm install
npm run dev
```

---

## 🚚 2. 配送员端小程序 (delivery-client)

### 技术栈
- **框架**: Remax
- **语言**: TypeScript + React

### 页面结构
```
src/
├── pages/
│   ├── task-list/         # 配送任务列表
│   ├── task-detail/       # 任务详情
│   ├── delivery-confirm/  # 送达确认
│   ├── history/           # 历史记录
│   └── my/                # 我的
├── services/
│   └── delivery.ts       # 配送 API
└── utils/
    └── request.ts        # 请求封装
```

### 核心功能
- ✅ 上线/下线状态切换
- ✅ 配送任务列表
- ✅ 地图导航
- ✅ 拍照送达确认
- ✅ 历史配送记录

### 启动命令
```bash
cd frontend/delivery-client
npm install
npm run dev
```

---

## 💻 3. 管理后台 (admin-client)

### 技术栈
- **框架**: React 18 + Vite
- **UI 库**: Ant Design 5
- **路由**: React Router 6
- **HTTP**: Axios
- **语言**: TypeScript

### 页面结构
```
src/
├── pages/
│   ├── login/            # 登录页
│   ├── dashboard/        # 仪表盘
│   ├── products/         # 产品管理
│   ├── orders/           # 订单管理
│   ├── delivery/         # 配送管理
│   └── statistics/       # 营收统计
├── components/
│   └── Layout.tsx       # 布局组件
├── services/
│   ├── product.ts       # 产品 API
│   └── order.ts         # 订单 API
├── utils/
│   └── request.ts       # Axios 封装
├── App.tsx              # 路由配置
└── main.tsx             # 应用入口
```

### 核心功能
- ✅ 登录认证
- ✅ 产品管理（增删改查）
- ✅ 订单管理（查看、分配配送）
- ✅ 配送员管理
- ✅ 营收统计与报表

### 启动命令
```bash
cd frontend/admin-client
npm install
npm run dev         # 开发环境: http://localhost:3000
npm run build       # 生产构建
```

---

## 🔧 开发说明

### API 配置
所有前端项目的 API 地址默认指向 `http://localhost:8080/api`

#### 用户端/配送员端小程序
修改 `src/utils/request.ts`:
```typescript
const API_BASE_URL = 'http://localhost:8080/api';
```

#### 管理后台
修改 `vite.config.ts` 代理配置:
```typescript
proxy: {
  '/api': {
    target: 'http://localhost:8080',
    changeOrigin: true
  }
}
```

### TypeScript 错误说明
创建的文件中存在的 TypeScript 错误是正常的，因为：
1. 依赖包尚未安装 (`npm install`)
2. 这些是占位代码，需要根据实际需求完善

### 待实现功能 (TODO)
- [ ] 用户登录（微信授权）
- [ ] 产品列表加载与展示
- [ ] 订单创建与支付
- [ ] 地址管理完整流程
- [ ] 配送任务分配与确认
- [ ] 图表与数据可视化
- [ ] 文件上传（产品图片、送达照片）

---

## 📦 依赖安装

在使用前，需要分别进入各项目目录安装依赖：

```bash
# 用户端小程序
cd frontend/user-client && npm install

# 配送员端小程序
cd frontend/delivery-client && npm install

# 管理后台
cd frontend/admin-client && npm install
```

---

## 🎯 下一步
1. 安装各项目依赖
2. 启动后端服务 (Spring Boot)
3. 配置微信小程序 AppID
4. 实现具体业务逻辑
5. 联调接口
