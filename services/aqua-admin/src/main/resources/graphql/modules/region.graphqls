# Region queries (for admin)

extend type Query {
  # Region queries
  regions(level: Int, parentCode: String): [Region!]!
  region(code: String!): Region
  # 获取所有地区数据，用于前端构建树形结构
  allRegions: [Region!]!
}
